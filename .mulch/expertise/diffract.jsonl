{"type":"reference","name":"Diffract CLI Commands","description":"Diffract is our branded UI/CLI layer on top of NemoClaw. 'diffract' is a symlink to 'nemoclaw' — all commands identical. Setup: diffract onboard, diffract list, diffract status. Sandbox: diffract <name> connect/status/logs/policy-add/policy-list/destroy. Credentials: diffract credentials list/reset <KEY>. Services: diffract start/stop. Other: diffract debug, diffract uninstall, diffract --version. Web UI project is in nemoclaw-ui/ (Next.js, package name 'diffract').","classification":"tactical","recorded_at":"2026-04-12T11:24:37.454Z","tags":["cli","commands"],"id":"mx-9236ac"}
{"type":"decision","title":"Diffract is branded layer over NemoClaw + OpenShell","rationale":"Diffract is our product name. CLI is a symlink (diffract -> nemoclaw). Web UI (Next.js) in nemoclaw-ui/ replaces interactive CLI with single-form deploy + dashboard. Underlying NemoClaw env vars (NEMOCLAW_*) preserved since that's what the CLI reads internally. Enterprise target: SSO, RBAC, multi-tenant planned.","classification":"tactical","recorded_at":"2026-04-12T11:24:46.258Z","tags":["branding","architecture"],"id":"mx-2a96a7"}
{"type":"pattern","classification":"foundational","recorded_at":"2026-06-06T17:41:25.007Z","evidence":{"commit":"ee41c4babd334683bf5164fd8ad73605ba701543"},"name":"chat-ws-loopback-reorigination-hop","description":"Chat /chat WebSocket on the dashboard (port 9119) requires a LOOPBACK client peer (web_server.py _ws_client_is_allowed, a DNS-rebinding defence). Behind Caddy a plain host socat forwards from the docker BRIDGE IP -> non-loopback peer -> WS upgrade 403 -> chat dead. An openshell/ssh -L tunnel does NOT fix it: the dashboard is docker-exec'd into the CONTAINER netns while the openshell ssh session lands in the sandbox WORKLOAD netns -> tunnel binds but 502s (no listener there). FIX (transport-only, no rebuild): in-container socat hop re-originates from the container's own 127.0.0.1 so the dashboard sees a loopback peer. Chain host 127.0.0.1:9119 -> CONTAINER_IP:9118 (bridge) -> 127.0.0.1:9119 (in container). Host listener bound to 127.0.0.1 (Caddy dials loopback). Lives in setup.sh sandbox-port-forwarder.sh with an end-to-end curl keepalive. Caveat: behind a reverse proxy the peer is always the proxy, so this neutralises the peer check in effect; real gates are the per-session token + Origin check. Verified 403->101 through Caddy + survives cold systemctl restart (commit ee41c4b).","files":["setup.sh"],"id":"mx-662460"}
{"type":"failure","classification":"foundational","recorded_at":"2026-06-06T17:59:17.951Z","evidence":{"commit":"41909ca8985e7177836f7b5607b996e81db364fc"},"description":"Dashboard /chat shows 'Chat unavailable: the ptyprocess package is missing' even though the WS handshake is 101. The embedded TUI chat (--tui) spawns a PTY via hermes_cli/pty_bridge.py needing ptyprocess; PtyBridge.spawn() raises PtyUnavailableError and web_server.py:3517 sends the error as a TEXT FRAME over an already-established socket -> WS 101 does NOT prove chat works (never discriminated this bug). Root cause: NemoClaw/agents/hermes/Dockerfile.base HERMES_UV_EXTRAS defaulted to 'messaging web' (no pty), so uv sync --frozen never installed the [pty] extra (uv.lock pins ptyprocess==0.7.0 under extra==pty). CHAT endpoint is /api/pty (NOT /api/ws).","resolution":"(1) Dockerfile.base HERMES_UV_EXTRAS='messaging web pty' (root cause, baked on base rebuild, covers fresh client deploys); (2) setup.sh sandbox-port-forwarder.sh presence-checked idempotent 'pip install ptyprocess==0.7.0' before the --tui launch (self-heals containers recreated by redeploys without a rebuild; no-op once image carries pty). Verify by reading the post-101 frame on /api/pty: working chat streams terminal output (Diffract TUI banner), broken sends 'Chat unavailable'. Commit 41909ca.","id":"mx-feda84"}
{"type":"pattern","classification":"foundational","recorded_at":"2026-06-07T11:56:25.027Z","evidence":{"commit":"5a961fe1d0d52a19956dd11a70471776600c9373"},"name":"dashboard-hermes-rebrand-safe-sweep","description":"Removing visible Hermes branding from the dashboards. SAFE RULE: replace the standalone capital-H brand word via perl s/\\bHermes\\b/Diffract/g ONLY on pure-UI-text files: hermes/web/index.html (the title tag), hermes/web/src/i18n/*.ts (16 locales; the word boundary spares camelCase KEYS like updateHermes/updatingHermes so only string VALUES change), hermes/web/src/themes/presets.ts (Hermes Teal). Plus targeted edits for the session-token error strings in hermes/web/src/lib/api.ts and gatewayClient.ts, and diffractui Dashboard.tsx (Hermes Web Dashboard). MUST NOT touch (a blanket sed corrupts these contracts): the X-Hermes-Session-Token HTTP header (api.ts:31), window.__HERMES_* globals, HERMES_BASE_PATH, the /api/hermes/ routes, the HERMES_QWEN_ env prefix. Left truthful because they are real things (do not fake): the nemohermes CLI command (diffractui Dashboard.tsx:301), the ~/.hermes config paths plus hermes CLI command refs shown in the Env/Config/Plugins/Skills screens, and code/CSS comments. tsc and npm run build catch any accidental key rename. DEPLOY CHAINS: agent dashboard (hermes/web) builds to hermes_cli/web_dist; the LIVE source of truth is the HOST agent dir /usr/local/lib/hermes-agent (the sandbox-port-forwarder docker-cps its web_dist into the container on every restart), NOT the repo - so rebuild THERE then docker cp into the container /opt/hermes/web_dist. diffractui runs next start from the repo at /root/diffract-main/diffractui - git pull, npm run build (.next), systemctl restart diffractui. GOTCHA fixed: setup.sh _overlay_host copied web/src but not web/index.html, which is why the browser title survived the earlier rebrand. Verify by grepping the BUILT bundle / .next AND the live served /agent/ title, not just that it built. Commits 12a1cf9 (source) and the live redeploy.","files":["setup.sh"],"id":"mx-5a1699"}
{"type":"decision","classification":"tactical","recorded_at":"2026-06-07T12:22:52.262Z","evidence":{"commit":"b38d9c578011f12e06bdf095d83cae916b72a719"},"title":"Genericize ~/.hermes paths/command refs in dashboard UI (not fake, not leave)","rationale":"User wanted NO visible hermes in the dashboard but the ~/.hermes config paths and hermes/nemohermes command refs are real. Faking them as ~/.diffract would show paths that do not exist on the box; leaving them keeps hermes visible. Chosen middle path: genericize - strip the ~/.hermes/ prefix so paths read config.yaml / plugins/ / skills/ (truthful, just relative), remove the parenthetical CLI-parity notes, and reword English (Remove this plugin?, No skills found.). Applied across all 17 locales incl full-width Chinese parens + a Hungarian declension. Left ONLY invisible contracts (X-Hermes-Session-Token, __HERMES_* globals, /api/hermes routes, HERMES_QWEN_, hermes_home field), the nemohermes CLI command, and code comments. STILL OPEN: HERMES_DOCS_URL=https://hermes-agent.nousresearch.com/docs/ (the Docs tab) is an external site, not a label - flagged to user. Commits 12a1cf9 + b38d9c5. See [[dashboard-hermes-rebrand-safe-sweep]].","id":"mx-236a4f"}
{"type":"pattern","classification":"foundational","recorded_at":"2026-06-07T13:04:16.196Z","evidence":{"commit":"1d2af4d229b8723989c7c3c8ede0c9b99619947c"},"name":"agent-self-identity-soul-md","description":"The chat agents self-identity (what it says when greeting, e.g. I am Hermes Agent running in the NemoClaw sandbox) is driven by SOUL.md at HERMES_HOME (= /sandbox/.hermes/SOUL.md in the container). SOUL.md is slot #1 of the system prompt (agent/system_prompt.py via load_soul_md in agent/prompt_builder.py) and OVERRIDES DEFAULT_AGENT_IDENTITY. The deployment SOUL.md is baked by NemoClaw/agents/hermes/Dockerfile (was an inline printf with Hermes/NemoClaw/OpenShell text). A SECOND, code-level source of the word Hermes Agent is HERMES_AGENT_HELP_GUIDANCE (prompt_builder.py), always appended in system_prompt.py. KEY FINDING (verified with hermes -z one-shot): a strong positive slot-#1 SOUL.md dominates BOTH the help-guidance Hermes mention AND the models inference of NemoClaw/OpenShell from env vars (NEMOCLAW_*, OPENSHELL_*) - so you do NOT need to edit upstream agent Python. FIX: edit SOUL.md only (data, safe to apply live; NEVER live-patch the agent Python/TUI - version skew ends the session). Use a positive identity (advisor: do not enumerate forbidden brands - that primes them) plus a generic do-not-describe-the-platform line. Single source of truth: NemoClaw/agents/hermes/SOUL.md, COPYd by the Dockerfile (fresh deploys) and re-applied live by setup.sh (host stage + self-healing forwarder step that overwrites an old-marker SOUL on each container start, idempotent/self-deactivating). VERIFY with hermes -z greeting + an adversarial who/where probe, not by cat-ing the file. Commit 1d2af4d. Related [[dashboard-hermes-rebrand-safe-sweep]].","files":["setup.sh"],"id":"mx-a35309"}
{"type":"pattern","classification":"foundational","recorded_at":"2026-06-07T14:47:55.624Z","evidence":{"commit":"f46d76edd00202f106640e3cea41b2117edff448"},"name":"dashboard-chat-via-gateway-v1-api","description":"The agent dashboard /agent Chat tab can be a normal message-bubble chat (not the xterm TUI). The dashboard backend (web_server.py) only exposes /api/pty (the terminal), but the agent also runs an OpenAI-compatible gateway on :8642 that Caddy proxies at origin /v1/* (handle /v1/* reverse_proxy 127.0.0.1:8642). That gateway runs the FULL agent (tools/skills/SOUL; ~14.5k prompt tokens) with NO auth (API_SERVER_KEY unset, /v1/models returns 200) and standard OpenAI SSE streaming (data: {choices:[{delta:{content}}]}), model id hermes-agent. So a chat UI is FRONTEND-ONLY: rewrite hermes/web/src/pages/ChatPage.tsx to POST /v1/chat/completions (leading slash = origin root, NOT under the /agent base path) with stream:true and render bubbles; keep the { isActive } prop so App.tsx persistent chat host is unchanged. Deploy like any web change (rebuild web_dist on the host agent dir + docker cp into the container) - no agent image rebuild. The Chat tab visibility is gated by __HERMES_DASHBOARD_EMBEDDED_CHAT__ (set true by hermes dashboard --tui), which stays true. Removing the TUI also removes its Hermes Agent welcome banner. Commit on this work.","files":["hermes/web/src/pages/ChatPage.tsx"],"id":"mx-66dd20"}
{"type":"failure","classification":"foundational","recorded_at":"2026-06-07T16:33:11.057Z","evidence":{"commit":"4961c3a96e0fc46d545674ce9239213429845fe7"},"description":"The chat UI backend (agent OpenAI-compatible gateway, Caddy /v1/* -> 127.0.0.1:8642) returns 502 on fresh sandboxes because the gateway runs in the sandbox WORKLOAD netns, not the container bridge netns. The gateway is started by nemoclaw-start over the openshell ssh session; its api_server listens on 127.0.0.1:18642 in the workload netns, fronted by an in-sandbox socat on :8642 (also workload netns). docker exec lands in the CONTAINER (bridge) netns and CANNOT see 8642/18642 (ss shows nothing, curl 000) - that is the diagnostic tell. setup.sh port-forwarder used socat TCP-LISTEN:8642 -> CONTAINER_IP:8642 (bridge), which cannot reach the workload netns -> 502. It worked only when onboards own openshell forward won the :8642 bind race over the socat (flaky). This is the SAME netns split as the 9119 chat-WS issue, opposite direction (9119 dashboard = container netns -> in-container loopback hop; 8642 gateway = workload netns -> ssh -L tunnel).","resolution":"Replace the 8642 socat in setup.sh sandbox-port-forwarder.sh with an openshell loopback forward (openshell forward start --background 8642 SANDBOX, an ssh -L that lands in the workload netns) + a keepalive (re-establish if the 127.0.0.1:8642 listener drops). Live unblock: kill the 8642 socat, openshell forward start --background 8642 hermes, then curl 127.0.0.1:8642/health = 200. Verified durable across a forwarder restart. Commit on this work.","id":"mx-164f99"}
{"type":"failure","classification":"foundational","recorded_at":"2026-06-07T16:49:06.860Z","evidence":{"commit":"3a8c452b8d4bd6b562fd09c711b53f7e4a683cae"},"description":"After fixing the 8642 502, the chat UI still failed with 403. Cause: the agent gateways api_server rejects requests that carry a browser Origin (and/or Referer) header as a DNS-rebinding/CSRF defence, and its allowed-origins list does not include the public host. Diagnostic tell: server-side curl to /v1/chat/completions with NO Origin returns 200, but the same request WITH -H Origin: https://<host> returns 403 (response header server: aiohttp = the gateway, not Caddy). Browsers always send Origin on JSON POSTs, so the same-origin chat UI is always 403d.","resolution":"Strip Origin (and Referer) on Caddys /v1/* reverse_proxy: add header_up -Origin and header_up -Referer inside the handle /v1/* reverse_proxy block (both the IP and domain CADDY_CONFIG variants in setup.sh). Caddy is a trusted proxy fronting the gateway; same-origin is the only legit client; cross-origin reads stay blocked by the browser CORS (gateway sends no ACAO). Live: edit /etc/caddy/Caddyfile + systemctl reload caddy; verify POST /v1 with an Origin header is 200. SECURITY NOTE: /v1 is unauthenticated, so this removes the browser-origin guard for it - fine for the same-origin dashboard behind the admin gate, revisit if /v1 is exposed more broadly. Related [[gateway-8642-workload-netns-socat-502]] and [[dashboard-chat-via-gateway-v1-api]].","id":"mx-295541"}
{"type":"decision","classification":"tactical","recorded_at":"2026-06-07T18:46:29.284Z","evidence":{"commit":"cc3750454b5caef91db2b9ba100f78d92a70efa1"},"title":"Persist dashboard chat conversations in localStorage, not server sessions","rationale":"User wanted chat history to survive reload and to revisit old conversations. The agent gateway /v1/chat/completions is STATELESS (no session id returned to continue a conversation), and the agents own sessions (/api/sessions, source=api_server) live inside the sandbox and are DESTROYED whenever the sandbox is recreated - which this user does frequently. So server sessions cannot reliably back the chat history. Chose client-side localStorage (key diffract.chat.v1: conversations[] + activeId) in hermes/web ChatPage.tsx: a conversation sidebar (new/select/delete) + persist on every change. Survives reloads AND sandbox recreations (browser-side). History is resent to /v1 each turn (OpenAI-style). Tradeoffs: per-browser (not cross-device), and continuing an old chat after a recreate is answered from the visible transcript only (fresh agent, no server memory). If cross-device/server-backed history is needed later, use the OpenAI Responses API (/v1/responses exists, stateful) or /api/sessions, accepting they vanish on sandbox destroy. Frontend-only, no backend/image change. See [[dashboard-chat-via-gateway-v1-api]].","id":"mx-b9d284"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-06-07T19:27:44.353Z","evidence":{"commit":"907c91f007c26ebd68408d40edb5771d3010af9f"},"name":"diffractui-files-api","description":"Files tab API (diffractui/src/app/api/files/route.ts) lets an admin browse/upload/download/delete files in the agent sandbox. Root-on-host gated only by admin session, so: re-verify session per method (requireSession, defense-in-depth on top of proxy.ts), use execFile/spawn with argv arrays and NO shell, confine every path to /sandbox with path.posix.normalize + prefix check (rejects ..), block writes/deletes to /sandbox/.hermes and deletion of /sandbox root, cap uploads at 100MB checking Content-Length before buffering. Upload flow: host temp file then docker cp then docker exec chown sandbox:sandbox + chmod 0644 (docker cp lands as root). CRITICAL: resolve sandbox name to container via the exact docker label openshell.ai/sandbox-name=NAME (plus openshell.ai/managed-by=openshell), NOT by container name. OpenShell names containers openshell-NAME-UUID, so a name=^openshell-NAME$ filter 404s for the real nemoclaw name like hermes. The UI passes the nemoclaw sandbox name (e.g. hermes); status route hides this with a detectSandbox fallback, files API resolves by label. Verified live: agent read an uploaded file via /v1.","files":["diffractui/src/app/api/files/route.ts"],"id":"mx-727cb9"}
{"type":"decision","classification":"tactical","recorded_at":"2026-06-08T03:22:23.053Z","evidence":{"commit":"7f9c81c6170cb85926ae9ad40d0106bfb9342b8f"},"title":"Persist sandbox CLI tools by baking them into the hermes image","rationale":"OpenShell sandboxes have no volume/bind-mount (by design); /sandbox is the container writable layer and is destroyed on nemoclaw destroy. nemoclaw onboard REBUILDS the sandbox image on each create from agents/hermes/Dockerfile (confirmed: onboard resolves AGENTS_DIR = ROOT/agents where ROOT = resolve(dist/lib,..,..) = the installed CLI repo root; the bundled ~/.nemoclaw/source is NOT used at runtime). So the durable way to persist a tool is to add a build layer to that Dockerfile. GHL CLI is baked via git clone of public hrubee/GHL-CLI then npm ci + npm run build then symlink ghl into /usr/local/bin (on PATH). node/npm/git are in the base image hermes-sandbox-base and build-time network works. node_modules retained (dist/index.js needs it). Takes effect on next sandbox recreate only. Working-file (user data) persistence is a SEPARATE problem: no volumes means it must be host-backed (restore-on-boot + backup-on-destroy); never restore /sandbox/.hermes (rebuilt at image build + guarded by /etc/nemoclaw/hermes.config-hash trust anchor in nemoclaw-start.sh).","id":"mx-cf5465"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-06-08T03:39:35.713Z","evidence":{"commit":"0c20ffdeb84505a64cb540ed0ebc2cd0e4c146c5"},"name":"sandbox-file-persistence","description":"Persist a sandbox user working files across destroy/recreate via host-backed tar (no volumes exist). scripts/diffract-persist.sh (installed to /usr/local/bin by setup.sh) has backup/restore/backup-if-ready subcommands; resolves the container by openshell.ai/sandbox-name label; tars /sandbox to /var/lib/diffract/persist/NAME/home.tar.gz excluding .hermes .nemoclaw .cache .npm node_modules GHL-CLI. Safety: restore runs once per container lifetime gated by in-container marker /sandbox/.diffract-restored (never clobbers a running sandbox newer files); periodic backup is gated on the marker so an empty fresh sandbox cannot overwrite good host data; backups atomic via tmp+mv and never overwrite a good archive with an empty/failed one; never touches .hermes. Wired in: setup.sh forwarder does restore-on-boot + backup-if-ready every ~5min in keepalive; diffractui deploy DELETE handler does best-effort backup-before-destroy. The live forwarder is kept in sync with setup.sh by extracting its heredoc with awk. Verified live without destroy: backup excludes correct, restore preserves sandbox:sandbox ownership, marker gating works.","files":["diffractui/src/app/api/deploy/route.ts"],"id":"mx-8221d1"}
{"type":"failure","classification":"tactical","recorded_at":"2026-06-08T06:55:57.125Z","evidence":{"commit":"40571d008709f179e800f357a1807d03c6135e46"},"description":"OpenShell confines the sandboxed agent workload filesystem READS to an allowlist: it can read its home /sandbox and runtime /opt/hermes, but NOT arbitrary image paths. A CLI baked into /opt/diffract-tools (world-readable -rwxr-xr-x, traversable parents) is still EACCES when the agent (uid 998 sandbox) tries to open it via openshell sandbox exec, even though docker exec into the outer container reads it fine. The agent workload is a separate containerd-overlay context from docker exec; /sandbox is shared+writable (files written via docker exec are visible to the workload), /opt and /usr/local/bin come from the image. Diagnosis: openshell sandbox exec --name hermes -- sh -lc reading /opt/hermes = OK, /opt/diffract-tools = FAIL. Fix shipped: universal installer install-diffract-tools.sh bakes tools into /sandbox/.diffract-tools/<name> + symlinks /usr/local/bin/<bin>; registry diffract-tools.json. NOTE: openshell sandbox exec rejects newline chars in the command arg (put commands on one line).","resolution":"Install agent-facing tools under /sandbox (agent home), not arbitrary paths like /opt; symlink the entrypoint onto PATH at /usr/local/bin. Verified via openshell sandbox exec.","id":"mx-08072c"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-06-08T07:22:07.782Z","evidence":{"commit":"12e3157ba43126b566ac8bc8c2c729b74ffc60a2"},"name":"universal-tool-connection","description":"Universal secure mechanism to connect a third-party CLI to the sandboxed agent so it can call the tool API without holding the secret. Validated end-to-end with GHL CLI (agent listed real GoHighLevel contacts using only placeholders). Three layers, registry-driven via NemoClaw/agents/hermes/diffract-tools.json: (1) INSTALL - install-diffract-tools.sh bakes each tool into /sandbox/.diffract-tools/NAME (MUST be /sandbox; OpenShell confines agent reads) + symlinks /usr/local/bin/BIN; optional patch step for proxy-compat fixups. (2) CREDENTIALS - register an OpenShell generic provider holding secretEnv + non-secret configEnv; sandbox sees only placeholders openshell:resolve:env:..., the L7 proxy substitutes real values at egress in BOTH headers and query params. detach+re-attach is REQUIRED to inject newly-added credentials into a running sandbox. (3) EGRESS - policy update SANDBOX --add-endpoint HOST:443:full --binary BIN --wait per apiHost. CRITICAL: on OpenShell 0.0.39 egress is denied unless the peer binary is named explicitly (--binary /usr/local/bin/node for node tools, /usr/bin/curl); empty binaries fail-closed. PROXY-COMPAT: the sandbox routes egress through an MITM proxy (NODE_EXTRA_CA_CERTS=/etc/openshell-tls/openshell-ca.pem); node undici/fetch and curl work, but axios legacy adapter aborts (stream has been aborted) - fix axios tools with adapter:fetch. scripts/diffract-tool-connect.sh automates layers 2-3 from the registry (secret read from env, never argv).","files":["NemoClaw/agents/hermes/diffract-tools.json"],"id":"mx-8d19ac"}
{"type":"decision","classification":"tactical","recorded_at":"2026-06-08T07:30:02.510Z","evidence":{"commit":"10d87979af4e7cfd4d230e942c12fae18f97c0eb"},"relates_to":["mx-8d19ac"],"title":"tool-connection-security-scoping","rationale":"Honest go-to-market boundary: we proved the agent cannot leak a tool secret, but we did NOT prove it can only reach the allowlisted host, so the two claims must be sold separately to avoid overstating the boundary.","id":"mx-95370a"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-06-08T07:30:19.935Z","evidence":{"commit":"10d87979af4e7cfd4d230e942c12fae18f97c0eb"},"relates_to":["mx-8d19ac"],"name":"universal-installer-clean-build-verified","description":"The universal tool-installer path is verified by a clean from-scratch docker build, not just inferred. On the VPS, docker build -f NemoClaw/agents/hermes/Dockerfile --build-arg BASE_IMAGE=ghcr.io/nvidia/nemoclaw/hermes-sandbox-base:latest -t diffract-tools-verify:test . completed end-to-end (image 164e3ea9, 3.81GB). Build step 11/32 ran install-diffract-tools.sh, which cloned hrubee/GHL-CLI, applied the axios adapter:fetch proxy-compat patch, ran npm ci plus npm run build, and symlinked /usr/local/bin/ghl. Smoke test: docker run --rm --entrypoint /usr/local/bin/node diffract-tools-verify:test /usr/local/bin/ghl --version prints 0.1.0 (rc 0). This closes the previously-unproven fresh-recreate gap: a brand-new sandbox image carries the baked tool on PATH without any manual /sandbox placement. Adding a tool is one registry entry in NemoClaw/agents/hermes/diffract-tools.json - no Dockerfile edit. Note: build emits two harmless SecretsUsedInArgOrEnv warnings for NEMOCLAW_PROVIDER_KEY (pre-existing, unrelated to the tool installer).","files":[".DS_Store",".mulch/expertise/diffract.jsonl"],"id":"mx-94af62"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-06-08T08:43:20.660Z","evidence":{"commit":"3b14309f57362ad0d2579447021c844af6b25401"},"relates_to":["mx-8d19ac"],"name":"tool-advertise-skill-discovery","description":"ADVERTISE is the 4th layer of universal-tool-connection - it makes the sandboxed hermes agent AWARE of a baked CLI. Install plus connect plus egress make a tool runnable and credentialed but do NOT make the agent discover it: hermes discovers capabilities from its skill catalog and nothing put the tool there, so a fresh session would not know ghl exists (it only ran before because it was told to). MECHANISM verified on the live box: hermes auto-discovers any SKILL.md dropped under /sandbox/.hermes/skills - hermes skills list shows it enabled with NO .bundled_manifest edit; the curator (hermes curator run) is only an optional usage-based optimizer, not a discovery gate (dry-run reported 0 candidates, skill already enabled). IMPLEMENTATION: advertise-diffract-tools.sh reads diffract-tools.json and emits one SKILL.md per tool (with an optional skill block: name/title/summary/tags/examples) into /sandbox/.hermes/skills/diffract-tools/NAME. It runs in the Dockerfile AFTER the .hermes setup (skills dir and SOUL.md only exist late; skills are NOT part of the config-integrity hash so it is safe after the hash pin). The generated skill body tells the agent the tool is PRE-AUTHENTICATED - run it directly, the env value is an openshell:resolve placeholder by design, never ask the user for a key (which would defeat confidentiality). SOUL.md also gets a generic always-in-context line; it is generic (not per-tool) so it survives the live setup.sh SOUL forwarder re-apply. PROVEN by clean docker build: build step 33/34 ran advertise, and a fresh container from the image lists ghl-crm enabled and has the baked SKILL.md and SOUL line. Adding a tool's discovery = a skill block in the registry; no code edit.","files":[".DS_Store"],"id":"mx-1064eb"}
{"type":"reference","classification":"tactical","recorded_at":"2026-06-08T09:12:36.854Z","evidence":{"commit":"c715d93fc5fec4e34025d6da31908f6c34296343"},"relates_to":["mx-8d19ac"],"name":"how-agent-discovers-tools-remote-llm","description":"How the sandboxed agent KNOWS it has a baked tool, given the LLM runs remotely (not in the sandbox). The agent has two halves: the BRAIN (LLM) runs remotely on NVIDIA inference and never touches the sandbox filesystem; the HARNESS (hermes runtime) runs INSIDE the sandbox, holds the files (SOUL.md, skills) and executes tool calls. The bridge is system-prompt assembly done by the harness: AIAgent._build_system_prompt in /opt/hermes/agent/prompt_builder.py calls build_skills_system_prompt, which scans every ENABLED, platform-compatible SKILL.md (skipping names in the disabled set), takes each frontmatter name + description, and compiles a compact skill index. SOUL.md identity plus that skill index are injected into the system prompt sent to the remote model on every session. So the model knows a tool exists because the in-sandbox harness narrates it in the prompt each turn; the model never reads SKILL.md directly. The full SKILL.md body is pulled on demand (progressive disclosure) when the model engages the skill, keeping the always-present catalog compact. PROVEN deterministically on the live box: invoking build_skills_system_prompt (after clear_skills_system_prompt_cache) produced a 10681-char prompt containing the line - ghl-crm: List and manage GoHighLevel CRM data ... so the catalog the remote model receives includes our skill, not just the disk file. CACHE GOTCHA: the skill index has a two-layer cache - an in-process LRU per running session and a disk snapshot validated by mtime/size. Writing a SKILL.md by raw file write does NOT clear a running session in-process cache, so an already-open session will not see the new skill until it reloads; a NEW session or a fresh recreate from the baked image builds the prompt fresh and sees it. The clean way to add a skill to a live session is the hermes reload_skills command or the hermes skills CLI (which clears the cache), not a raw file write.","files":[".DS_Store"],"id":"mx-35ab38"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-06-08T09:56:36.927Z","evidence":{"commit":"e53aacda644be6290373faf38ec04ef71f2b94eb"},"relates_to":["mx-8d19ac"],"name":"diffract-agent-autonomous-tool-use-proven","description":"FULL end-to-end proof that the in-sandbox Diffract agent autonomously discovers AND uses a baked tool. Driven via hermes one-shot mode through the workload context: openshell sandbox exec --name hermes -- hermes -z PROMPT (using openshell sandbox exec, NOT docker exec, so the OpenShell egress proxy and credential injection apply; docker exec would lack them). The prompt was plain English (Look up my CRM contacts and summarize) and DID NOT name ghl. The agent on its own: (1) discovered the ghl-crm skill from the catalog the harness compiled into its system prompt, (2) chose to run ghl contacts list itself, (3) read real GoHighLevel data via the placeholder-to-egress-injection path (token never in sandbox), (4) answered in natural language (25 contacts, names, offered next steps). The live agent stack inside the sandbox: hermes gateway run (the always-on agent the dashboard talks to) plus hermes dashboard --host 0.0.0.0 --skip-build --insecure --tui (the Diffract UI chat backend). hermes -z is the clean headless way to drive the real agent loop for demos/tests and capture only the final answer. This confirms the discovery mechanism end-to-end through the real agent, not just by inspecting the built prompt. Security caveat unchanged: confidentiality proven; data-exfil via the raw-socket bypass is still gated on the OpenShell 0.0.57 upgrade.","files":[".DS_Store"],"id":"mx-29576f"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-06-08T10:28:18.044Z","evidence":{"commit":"2f04186fcb0327e1dc05ae1da6831a9a8edb67e7"},"relates_to":["mx-8d19ac"],"name":"tool-survives-sandbox-recreate-proven","description":"Confirmed by a REAL destroy/recreate on the live VPS (not just a clean build) that the universal tool stack survives. After the user destroyed and recreated the hermes sandbox: (1) the ghl binary came back baked - /usr/local/bin/ghl -> /sandbox/.diffract-tools/ghl/dist/index.js with a fresh build timestamp, proving nemoclaw onboard rebuilds the image from the updated agents/hermes/Dockerfile (install + advertise layers); (2) the ghl-crm skill was enabled in hermes skills list and the SOUL.md pre-authenticated line was present; (3) the global OpenShell provider ghl survived with its 2 credential keys (token + location) intact - providers are global/host-side, NOT sandbox state. The ONLY thing wiped is the per-sandbox connection: openshell sandbox provider list hermes showed No providers attached, and the egress policy reset. Re-connect needs NO token because the provider still holds it - just: openshell sandbox provider detach hermes ghl (no-op) then attach, then openshell policy update hermes --add-endpoint services.leadconnectorhq.com:443:full --rule-name ghl-api --binary /usr/local/bin/node --wait. After that, env in the sandbox showed GHL_PRIVATE_TOKEN and GHL_LOCATION_ID placeholders present, and the real Diffract agent (hermes -z one-shot, prompt did NOT name ghl) autonomously discovered the skill, ran ghl contacts list, and returned 25 real contacts with names. IMPROVEMENT CANDIDATE: auto-reconnect on boot (re-attach providers + re-apply tool egress policy from a host-side manifest) would make the connection survive too, closing the original tools-must-not-vanish ask fully.","files":[".DS_Store"],"id":"mx-38d201"}
